<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Nova Receita</title>
</head>
<body>
    <h1>Nova Receita</h1>
    
    <form action="/receitas/salvar" method="POST">
        <div>
            <label>Título:</label><br>
            <input type="text" name="titulo" required>
        </div>
        
        <div>
            <label>Descrição:</label><br>
            <textarea name="descricao" rows="4" cols="50" required></textarea>
        </div>

        <div>
            <label>Tempo de Preparo (min):</label><br>
            <input type="number" name="tempoPreparo" required>
        </div>

        <div>
            <label>Autor (Quem está criando?):</label><br>
            <select name="autorId" required>
                <option value="">Selecione um autor...</option>
                <% usuarios.forEach(usuario => { %>
                    <option value="<%= usuario.id %>"><%= usuario.nome %></option>
                <% }) %>
            </select>
        </div>
        <br>
        <button type="submit">Salvar Receita</button>
    </form>
    <a href="/receitas">Cancelar</a>
</body>
</html>