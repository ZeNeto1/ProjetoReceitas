<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Novo Usuário</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1><%= usuario ? 'Editar Usuário' : 'Criar Novo Usuário' %></h1>
    
    <form action="<%= usuario ? `/usuarios/${usuario.id}?_method=PUT` : '/usuarios' %>" method="POST">
        <div>
            <label for="nome">Nome:</label>
            <input type="text" id="nome" name="nome" value="<%= usuario ? usuario.nome : '' %>" required>
        </div>
        <br>
        <div>
            <label for="email">E-mail:</label>
            <input type="email" id="email" name="email" value="<%= usuario ? usuario.email : '' %>" required>
        </div>
        <br>
        <button type="submit">Salvar Usuário</button>

    </form>

    <a href="/usuarios">Cancelar</a>
</body>
</html>